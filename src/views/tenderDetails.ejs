<!DOCTYPE html>
<html lang="pl">
<head>
  <meta charset="UTF-8">
  <title>Lista trwających przetargów</title>
  <link rel="stylesheet" href="/style.css">
</head>
<h1><%= tender.title %></h1>
<p><%= tender.description %></p>
<p><strong>Instytucja zamawiająca:</strong> <%= tender.institution %></p>
<p><strong>Data rozpoczęcia:</strong> <%= tender.startDate %></p>
<p><strong>Data zakończenia:</strong> <%= tender.endDate %></p>

<% if (isActive) { %>
  <h3>Składaj ofertę:</h3>
  <form action="/tenders/<%= tender.id %>/offer" method="POST">
    <div>
      <label for="bidderName">Nazwa składającego ofertę:</label>
      <input type="text" id="bidderName" name="bidderName" required />
    </div>
    <div>
      <label for="offerValue">Wartość oferty:</label>
      <input type="number" id="offerValue" name="offerValue" required />
    </div>
    <button type="submit">Złóż ofertę</button>
  </form>
<% } else { %>
  <p>Przetarg zakończony, składanie ofert nie jest możliwe.</p>
<% } %>
<p><a href="/tenders">Powrót do listy aktywnych przetargów</a></p>
</html>