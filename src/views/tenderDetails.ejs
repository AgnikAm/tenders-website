<!DOCTYPE html>
<html lang="pl">
<head>
  <meta charset="UTF-8">
  <title><%= tender.title %></title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <h1><%= tender.title %></h1>
  <p><strong>Instytucja zamawiająca:</strong> <%= tender.institution %></p>
  <p><strong>Opis:</strong> <%= tender.description %></p>
  <p><strong>Od:</strong> <%= new Date(tender.startDate).toLocaleString('pl-PL') %></p>
  <p><strong>Do:</strong> <%= new Date(tender.endDate).toLocaleString('pl-PL') %></p>

  <% if (isActive) { %>
    <form action="/tenders/<%= tender.id %>/offer" method="get">
      <button type="submit">Złóż ofertę</button>
    </form>
  <% } else { %>
    <p>Przetarg zakończony.</p>
  <% } %>

  <p><a href="/tenders">Powrót do listy przetargów</a></p>
</body>
</html>
