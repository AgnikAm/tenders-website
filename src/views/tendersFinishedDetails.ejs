<% title = 'Szczegóły zakończonego przetargu' %>

<h1 class="mb-4">Szczegóły przetargu: <%= tender.title %></h1>
<p><strong>Opis:</strong> <%= tender.description %></p>
<p><strong>Instytucja zamawiająca:</strong> <%= tender.institution %></p>
<p><strong>Data zakończenia przyjmowania ofert:</strong> <%= new Date(tender.endDate).toLocaleString('pl-PL') %></p>
<p><strong>Budżet przetargu:</strong> <%= tender.maxBudget %> PLN</p>

<h2 class="mt-4">Oferty:</h2>

<% if (noValidOffers) { %>
  <div class="alert alert-danger">Wszystkie oferty przekraczają budżet. Przetarg zakończony bez rozstrzygnięcia.</div>
<% } else { %>
  <table class="table table-bordered">
    <thead class="table-light">
      <tr>
        <th>Numer oferty</th>
        <th>Nazwa oferenta</th>
        <th>Wartość oferty</th>
        <th>Data złożenia oferty</th>
      </tr>
    </thead>
    <tbody>
      <% offers.forEach((offer, index) => { %>
        <tr>
          <td><%= index + 1 %></td>
          <td><%= offer.bidderName %></td>
          <td><%= offer.offerValue %> PLN</td>
          <td><%= new Date(offer.submittedAt).toLocaleString('pl-PL') %></td>
        </tr>
      <% }) %>
    </tbody>
  </table>
<% } %>
